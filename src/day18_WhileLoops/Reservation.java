package day18_WhileLoops;

import java.util.Scanner;
/*
public class Reservation {}
  /*  public static void main(String[] args) {
        int King = 120;
        int Queen = 100;
        int Single = 80;
        int total = 0;

        Scanner input = new Scanner(System.in);

        while (true) ;
        {
            System.out.println("Which room you want to reserve ?");
            String roomtype = input.next().toLowerCase();
            while (!(roomtype.equals("queen") || roomtype.equals("king") || roomtype.equals("single"))) {
                System.err.println("invalid entry, re-enter valid entry");
                roomtype=input.nextLine();
            }


            System.out.println("How many nights are you planning to stay ?");
            int night = input.nextInt();


            System.out.println("Would you like to reserve another room ?");
            String answer = input.next().toLowerCase();
            while (!(answer.equals("yes") || answer.equals("no"))) {
                System.out.println("INVALID ANSWER");
            }


            if (answer.equals("yes") || answer.equals("no"))
                if (answer.equals("yes") && roomtype.equals("king")) {
                    total += King * night;
                }
            if (answer.equals("yes") && roomtype.equals("Queen")) {
                total += Queen * night;
            } else if (answer.equals("yes") && roomtype.equals("single")) {
                total += Single * night;
            }
            if (answer.equals("no")) {
                break;
  */